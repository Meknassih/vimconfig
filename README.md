# vimconfig

## Installation
```
mkdir -p ~/.config/nvim
git clone git@github.com:Meknassih/vimconfig.git ~/.config/nvim
nvim
```
Then after Packer installs on first startup, quit and restart Neovim.
